from setuptools import setup, find_packages

setup(
    name="teras",
    version="0.1.0",
    description="Keras model fit equivalent for pytorch",
    # package_dir={"": "src"},
    packages=("teras",),
)
